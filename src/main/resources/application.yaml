spring:
  application:
    name: aln

---
# ========================
# JWT (Bearer) Authentication
# ========================
security:
  jwt:
    # ?????? ??? ??????? ??????? (?? ????? ????????????? ?? ?????????? ????????? JWT_SECRET)
    secret: ${JWT_SECRET:change-me-dev-secret-at-least-32-bytes-long}

    # ????? ????? ??????? (????? "900s", "15m", "PT15M")
    access-ttl: 900s      # Access-?????: 15 ?????
    refresh-ttl: 7d       # Refresh-?????: 7 ????

---
# ========================
# ?????? (???? ? ??.)
# ========================
server:
  port: 8080
spring:
  web:
    error:
      include-message: always
